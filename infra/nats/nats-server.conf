# nats-server.conf — JetStream enabled

server_name: crm-nats

# Client connections
port: 4222

# HTTP monitoring
http_port: 8222

# JetStream storage
jetstream {
  store_dir: /data/jetstream
  max_memory_store: 1GB
  max_file_store:   10GB
}
